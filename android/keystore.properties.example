# Copy this file to android/keystore.properties only on the secure release-build machine.
# Do not commit the copied file or the signing key to source control.
storeFile=../release/lumae-upload-key.jks
storePassword=REPLACE_WITH_SECURE_STORE_PASSWORD
keyAlias=lumae-upload
keyPassword=REPLACE_WITH_SECURE_KEY_PASSWORD
