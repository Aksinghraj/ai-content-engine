import { useEffect, useState } from "react";
import { Database, Globe, Lock, Moon, ShieldCheck, Sun, Trash2, UserRound } from "lucide-react";
import { toast } from "sonner";
import DashboardLayout from "@/components/DashboardLayout";
import { TwoFactorSecurityPanel } from "@/components/TwoFactorSecurityPanel";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { useTheme } from "@/contexts/ThemeContext";
import { useAuth } from "@/_core/hooks/useAuth";
import { trpc } from "@/lib/trpc";

const languages = [
  ["en", "English"], ["hi", "Hindi"], ["hinglish", "Hinglish"], ["bho", "Bhojpuri"], ["bn", "Bengali"], ["gu", "Gujarati"], ["kn", "Kannada"], ["ml", "Malayalam"], ["mr", "Marathi"], ["pa", "Punjabi"], ["ta", "Tamil"], ["te", "Telugu"],
] as const;
type LanguageCode = (typeof languages)[number][0];

export default function SettingsAdvanced() {
  const [tab, setTab] = useState("account");
  const [language, setLanguage] = useState<LanguageCode>("en");
  const { user } = useAuth();
  const { theme, effectiveTheme, highContrast, setTheme, setHighContrast } = useTheme();
  const savedLanguage = trpc.accountPreferences.getLanguage.useQuery();
  const saveLanguage = trpc.accountPreferences.setLanguage.useMutation({ onSuccess: ({ language: saved }) => toast.success(`${languages.find(([code]) => code === saved)?.[1] || "Language"} saved.`), onError: (error) => toast.error(error.message || "Could not save the language.") });
  const deleteAccount = trpc.auth.account.deleteAccount.useMutation({ onSuccess: () => { toast.success("Your account was deleted."); window.location.assign("/login?accountDeleted=1"); }, onError: (error) => toast.error(error.message || "Could not delete your account.") });
  useEffect(() => { if (savedLanguage.data && languages.some(([code]) => code === savedLanguage.data)) setLanguage(savedLanguage.data as LanguageCode); }, [savedLanguage.data]);
  const chooseLanguage = (value: LanguageCode) => { setLanguage(value); saveLanguage.mutate({ language: value }); };
  const deleteCurrentAccount = () => { if (window.prompt("This permanently deletes your Lumae account and associated data. Type DELETE to continue.") === "DELETE") deleteAccount.mutate(); else toast.message("Account deletion cancelled."); };
  return <DashboardLayout><main className="mx-auto w-full max-w-6xl space-y-6 px-4 py-5 sm:px-6"><header><h1 className="text-3xl font-semibold text-foreground">Settings</h1><p className="mt-2 text-sm text-muted-foreground">Manage your real account preferences and security. Settings that are not yet supported are not shown as active.</p></header><Tabs value={tab} onValueChange={setTab} className="space-y-5"><TabsList className="grid h-auto w-full grid-cols-3 gap-1 bg-muted p-1"><TabsTrigger value="account"><UserRound className="mr-2 h-4 w-4" />Account</TabsTrigger><TabsTrigger value="security"><Lock className="mr-2 h-4 w-4" />Security</TabsTrigger><TabsTrigger value="data"><Database className="mr-2 h-4 w-4" />Data</TabsTrigger></TabsList>
    <TabsContent value="account" className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle className="flex items-center gap-2"><UserRound className="h-5 w-5 text-primary" />Account information</CardTitle><CardDescription>Values below come from your signed-in Lumae account.</CardDescription></CardHeader><CardContent className="space-y-4"><div><p className="text-xs text-muted-foreground">Name</p><p className="mt-1 font-medium text-foreground">{user?.name || "Not provided"}</p></div><div><p className="text-xs text-muted-foreground">Email</p><p className="mt-1 break-all font-medium text-foreground">{user?.email || "No email on this account"}</p></div><div><p className="text-xs text-muted-foreground">Account role</p><Badge variant="outline" className="mt-1 capitalize">{user?.role || "user"}</Badge></div></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><Globe className="h-5 w-5 text-primary" />Language</CardTitle><CardDescription>Your selection is stored securely for this account and used as the default in Content Generator.</CardDescription></CardHeader><CardContent className="space-y-3"><Label htmlFor="account-language">Preferred language</Label><Select value={language} onValueChange={(value) => chooseLanguage(value as LanguageCode)}><SelectTrigger id="account-language"><SelectValue /></SelectTrigger><SelectContent>{languages.map(([code, name]) => <SelectItem key={code} value={code}>{name}</SelectItem>)}</SelectContent></Select><p className="text-xs text-muted-foreground">Bhojpuri is available for new content-generation requests. Interface translations are introduced progressively where verified copy is available.</p></CardContent></Card><Card className="lg:col-span-2"><CardHeader><CardTitle>Appearance and accessibility</CardTitle><CardDescription>These controls save your display preference without inventing notification or tracking settings.</CardDescription></CardHeader><CardContent className="grid gap-4 md:grid-cols-2"><div className="rounded-md border border-border p-4"><div className="flex items-center gap-2"><span className="font-medium text-foreground">Display mode</span>{effectiveTheme === "dark" ? <Moon className="h-4 w-4 text-primary" /> : <Sun className="h-4 w-4 text-amber-500" />}</div><div className="mt-3 grid grid-cols-3 gap-2">{(["light", "dark", "auto"] as const).map((value) => <Button key={value} variant={theme === value ? "default" : "outline"} className={theme === value ? "lumae-gradient-cta capitalize" : "capitalize"} onClick={() => setTheme(value)}>{value === "light" ? "Bright" : value}</Button>)}</div></div><div className="flex items-center justify-between gap-4 rounded-md border border-border p-4"><div><p className="font-medium text-foreground">High contrast</p><p className="mt-1 text-xs text-muted-foreground">Improves text, borders, and focus visibility.</p></div><Button variant={highContrast ? "default" : "outline"} className={highContrast ? "lumae-gradient-cta" : ""} onClick={() => setHighContrast(!highContrast)}>{highContrast ? "Enabled" : "Enable"}</Button></div></CardContent></Card></TabsContent>
    <TabsContent value="security"><TwoFactorSecurityPanel /><Card className="mt-6"><CardHeader><CardTitle className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-primary" />Session information</CardTitle><CardDescription>Lumae does not display invented device sessions. Use your browser and identity-provider security settings to review active sessions.</CardDescription></CardHeader></Card></TabsContent>
    <TabsContent value="data"><Card><CardHeader><CardTitle>Account data</CardTitle><CardDescription>Data export is not presented until a verified export workflow is available. Account deletion is permanent.</CardDescription></CardHeader><CardContent><Button variant="destructive" onClick={deleteCurrentAccount} disabled={deleteAccount.isPending}>{deleteAccount.isPending ? "Deleting…" : <><Trash2 className="mr-2 h-4 w-4" />Delete account</>}</Button></CardContent></Card></TabsContent>
  </Tabs></main></DashboardLayout>;
}
