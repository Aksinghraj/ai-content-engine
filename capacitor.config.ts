import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aicontentengine.app',
  appName: 'AI Content Engine',
  webDir: 'dist'
};

export default config;
